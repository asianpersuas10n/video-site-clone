const express = require("express");
const router = express.Router();

// calling controller
const subscriptions_controller = require("../Controllers/subscripotions");

// routes

module.exports = router;
