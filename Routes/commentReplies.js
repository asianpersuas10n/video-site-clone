const express = require("express");
const router = express.Router();

// calling controller
const commentReplies_controller = require("../Controllers/commentReplies");

// routes

module.exports = router;
